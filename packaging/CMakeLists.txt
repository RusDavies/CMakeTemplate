set(CPACK_GENERATOR "RPM")

#set(CPACK_RPM_PACKAGE_VERSION "${CMAKE_PROJECT_VERSION}" )

#set(CPACK_RPM_PACKAGE_NAME ${CMAKE_PROJECT_NAME})

#set(CPACK_RPM_PACKAGE_SUMMARY ${PROJECT_SUMMARY})

#set(CPACK_RPM_PACKAGE_DESCRIPTION ${PROJECT_DESCRIPTION})

set(CPACK_RPM_PACKAGE_RELEASE 1)

#set(CPACK_RPM_PACKAGE_LICENSE ${PROJECT_LICENSE})

#set(CPACK_RPM_PACKAGE_CONTACT "${PROJECT_CONTACT_NAME} <${PROJECT_CONTACT_EMAIL}>")

#set(CPACK_RPM_PACKAGE_VENDOR ${PROJECT_VENDOR})

set(CPACK_RPM_PACKAGE_GROUP "Development/Tools/Other")

#set(CPACK_RPM_PACKAGE_ARCHITECTURE ${CMAKE_SYSTEM_PROCESSOR})

#set(CPACK_RPM_PACKAGING_INSTALL_PREFIX ${CMAKE_INSTALL_PREFIX})

#set(CPACK_RPM_PACKAGE_FILE_NAME "${CPACK_PACKAGE_NAME}-${CPACK_PACKAGE_VERSION}-${CPACK_PACKAGE_RELEASE}.${CMAKE_SYSTEM_PROCESSOR}")


configure_file("${CMAKE_CURRENT_SOURCE_DIR}/package.spec.in" "${CMAKE_CURRENT_BINARY_DIR}/${CMAKE_PROJECT_NAME}.spec" @ONLY IMMEDIATE)
set(CPACK_RPM_USER_BINARY_SPECFILE "${CMAKE_CURRENT_BINARY_DIR}/${CMAKE_PROJECT_NAME}.spec")


include(CPack)
